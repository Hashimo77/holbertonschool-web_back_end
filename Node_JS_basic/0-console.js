/**
 * Simple function to print a message to the standard output.
 * @param {string} message - The string to be printed.
 */
const displayMessage = (message) => {
  console.log(message);
};

module.exports = displayMessage;
